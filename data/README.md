# Statistiques de vues (non affichées sur le README profil)

- `profile-views.json` : historique quotidien (delta par rapport au total komarev précédent).
- Le graphique est généré dans `assets/profile-views-chart.svg`.

Mis à jour par le workflow **Profile views daily snapshot** (`.github/workflows/profile-views-daily.yml`).

Le compteur source est le même que sur le badge **komarev/ghpvc** du README : seule la **différence quotidienne** du total est enregistrée ici.
